import category_game from "../interface/Category_game";
import prisma from "../lib/prisma";

async function create(idGame: number, idCategory: number) {
    try {
        
    } catch (e) { 
        return {message: "Error creating", stauts: 404}
    }
}


